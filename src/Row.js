import React from "react";

function Row({ title }) {
    const [movies, setMovies] = useState([]);

    // Row = () =>{
    return ( <
        div >
        <
        h2 > { title } < /h2> < /
        div >
    );
}

export default Row;